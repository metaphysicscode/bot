[phases.setup]
nixPkgs = [
  "python311",
  "python311Packages.pip",
  "glib", "gtk3", "nss", "nspr", "cups", "atk", "at-spi2-core", "expat", "dbus", "pango", "cairo", "alsa-lib", "libxkbcommon", "xorg.libX11", "xorg.libXcomposite", "xorg.libXdamage", "xorg.libXext", "xorg.libXfixes", "xorg.libXrandr", "mesa"
]

[phases.install]
cmds = [
  "pip install --break-system-packages -r requirements.txt",
  "playwright install chromium"
]

[start]
cmd = "python app.py"
